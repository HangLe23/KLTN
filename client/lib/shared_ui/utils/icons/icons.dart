import 'package:client/index.dart';
import 'package:flutter/material.dart';

class CustomIcons {
  CustomIcons._();
  static Icon home = Icon(
    Icons.home,
    color: CustomColor.white,
    size: 20,
  );
  static Icon source = Icon(
    Icons.source,
    color: CustomColor.white,
    size: 20,
  );
  static Icon node = Icon(
    Icons.devices,
    color: CustomColor.white,
    size: 20,
  );
  static Icon monitoring = Icon(
    Icons.av_timer,
    color: CustomColor.white,
    size: 20,
  );
  static Icon setting = Icon(
    Icons.settings,
    color: CustomColor.white,
    size: 20,
  );
  static Icon notifiaction = Icon(
    Icons.notifications_none,
    color: CustomColor.white,
    size: 20,
  );
  static Icon timer = Icon(
    Icons.access_time_rounded,
    color: CustomColor.white,
    size: 20,
  );
  static Icon upload = Icon(
    Icons.file_open_outlined,
    color: CustomColor.purple,
    size: 25,
  );
}
